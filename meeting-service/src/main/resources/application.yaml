server:
  port: ${SERVER_PORT:12301}

spring:
  application:
    name: meeting-service
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true
